{"id":16,"course_id":8,"title":"Containers, Images, and Pods","summary":"Understand the unit Kubernetes actually runs and why images should be immutable.","sequence":1,"body_simplified":"## Plain-English model\nA container image is a packaged filesystem plus a startup command. A container is a running instance of that image. Kubernetes does not schedule a bare container; it schedules a Pod. A Pod is a small wrapper around one or more containers that share networking and lifecycle.\n- Build images once and promote the same image through environments.\n- Put environment differences in configuration, not in a different hand-built image.\n- Treat Pods as disposable. If a Pod dies, the controller creates a replacement.\n- Use one main application container per Pod unless sidecars solve a clear shared lifecycle problem.\n\n## Local-safe inspection\n$ kubectl get pods -A\n$ kubectl describe pod POD_NAME -n NAMESPACE\n$ kubectl get pod POD_NAME -n NAMESPACE -o yaml\nInspect the image, command, args, restart count, service account, labels, and events. If you do not have a cluster, read a sample Pod manifest and identify the same fields.\n\n## Common mistakes\nBeginners often debug by editing a live Pod. That change disappears because a Deployment owns the Pod template. Change the controller or chart instead.","body_traditional":"## Plain-English model\nA container image is a packaged filesystem plus a startup command. A container is a running instance of that image. Kubernetes does not schedule a bare container; it schedules a Pod. A Pod is a small wrapper around one or more containers that share networking and lifecycle.\n- Build images once and promote the same image through environments.\n- Put environment differences in configuration, not in a different hand-built image.\n- Treat Pods as disposable. If a Pod dies, the controller creates a replacement.\n- Use one main application container per Pod unless sidecars solve a clear shared lifecycle problem.\n\n## Local-safe inspection\n$ kubectl get pods -A\n$ kubectl describe pod POD_NAME -n NAMESPACE\n$ kubectl get pod POD_NAME -n NAMESPACE -o yaml\nInspect the image, command, args, restart count, service account, labels, and events. If you do not have a cluster, read a sample Pod manifest and identify the same fields.\n\n## Common mistakes\nBeginners often debug by editing a live Pod. That change disappears because a Deployment owns the Pod template. Change the controller or chart instead.","pinyin":"Scenario: A teammate says \"the container is broken\" after a Pod restarts.\nChecklist:\n- Confirm the image name and tag or digest.\n- Read restart count and Last State in the Pod description.\n- Check whether a Deployment, Job, or StatefulSet owns the Pod.\n- Decide whether the fix belongs in the image, manifest, or runtime config.\nCommands:\n$ kubectl describe pod POD_NAME -n NAMESPACE\n$ kubectl get pod POD_NAME -n NAMESPACE -o jsonpath='{.metadata.ownerReferences}'","audio_url":null,"video_url":null,"course_slug":"platform-kubernetes-fundamentals","course_category":"Kubernetes","course_era":"Platform Academy","vocabulary":[{"id":51,"simplified":"Image","traditional":"Image","pinyin":"Package","definition":"An immutable package that contains application code, filesystem layers, and startup metadata."},{"id":52,"simplified":"Container","traditional":"Container","pinyin":"Runtime","definition":"A running process created from an image with isolated filesystem, process, and network settings."},{"id":53,"simplified":"Pod","traditional":"Pod","pinyin":"Kubernetes unit","definition":"The smallest schedulable Kubernetes object, usually wrapping one application container."},{"id":54,"simplified":"Sidecar","traditional":"Sidecar","pinyin":"Pod pattern","definition":"A helper container that shares the Pod lifecycle, such as a proxy or log shipper."},{"id":55,"simplified":"Restart count","traditional":"Restart count","pinyin":"Failure signal","definition":"The number of times kubelet restarted a container inside the Pod."}],"flashcards":[{"id":31,"lesson_id":16,"prompt":"What is the difference between an image and a container?","answer":"An image is the package; a container is a running instance of that package.","pinyin":"platform","difficulty":"beginner"},{"id":32,"lesson_id":16,"prompt":"Why should you avoid editing an owned Pod directly?","answer":"The controller will recreate Pods from its template and your live edit will not become durable.","pinyin":"platform","difficulty":"beginner"},{"id":33,"lesson_id":16,"prompt":"What should you inspect first after a Pod restart?","answer":"Describe the Pod and check Last State, restart count, events, and logs.","pinyin":"platform","difficulty":"beginner"}]}
